# V128-P5.1 — TEACHER ASSIGNMENT RUNTIME TRUTH & SEMANTIC GAP AUDIT BOOT

MODE:
READ-ONLY / AUDIT-FIRST / NO IMPLEMENTATION

ROLE:
Architecture / Runtime Truth Auditor

OBJECTIVE:
Determine the actual current meaning and implementation of “Teacher Assignment” across DMA class, lesson-session, teacher, report, and parent-facing flows before proposing any code change.

CANON:
Teacher Assignment is an operational responsibility relationship.
Do NOT assume it is equivalent to:
- teacher account,
- school membership,
- class visibility,
- session creator,
- report author,
- last editor,
- or parent-facing display fallback.

FROZEN INVARIANTS:
1. Assignment ≠ Access.
2. Assignment ≠ Mutation Actor.
3. Class assignment and session assignment remain distinct until runtime evidence proves otherwise.
4. Reassignment must not silently rewrite historical completed-session attribution.
5. Substitute/session override must be evaluated explicitly.
6. Unknown assignment is a valid state unless the existing contract requires otherwise.
7. Displayed teacher must derive from a deterministic semantic source.
8. Assignment mutation authority must be explicit and server-enforced.

KNOWN CONTINUITY:
- P3 School Announcement is deployed and accepted.
- P4 Home Practice is deployed and accepted.
- P4 backend migration: `v128_p4_1_home_practice`.
- P4 accepted UX correction lineage includes `e6f77a74` → `db58e7a5`.
- Existing scheduling discovery previously found lesson-session writers authorized to admin or same-school `master_admin` / `sub_admin`; Teacher Lead was not authorized for that known writer path.
- Existing teacher read capability includes `get_teacher_classes`.
These are continuity facts only; RE-VERIFY any fact that materially affects P5.

MANDATORY EXECUTION ORDER:

A. RE-PIN AUTHORITATIVE HEAD
- Establish current authoritative HEAD before reading conclusions.
- Report SHA and whether unexpected drift exists.
- Do not mutate repository state.

B. SCHEMA / MIGRATION AUDIT
Search for all teacher-related fields, join tables, constraints, enums, triggers, audit records, and migrations related to:
- classes/groups;
- lesson sessions;
- session reports;
- teacher profiles;
- school memberships;
- class-teacher relationships;
- session-teacher relationships;
- assignment history.
For every candidate teacher field, identify what the schema/migration actually says it means.

C. RPC / AUTHORITY AUDIT
Inspect actual definitions and authorization for:
- `create_lesson_session`
- `update_lesson_session`
- `cancel_lesson_session`
- `get_school_week_schedule`
- `get_teacher_classes`
- `submit_session_report`
- class/session detail readers
- any RPC or mutation that writes a teacher/class/session association.
Do not infer authority from the frontend.

D. FRONTEND WIRING AUDIT
Inspect actual production component/route wiring in:
- `/school/schedule`
- `/school/manage`
- teacher class surfaces
- teacher session/detail surfaces
- Teacher Closeout
- parent session outcome
- parent journal/session detail
- class create/edit surfaces.
For every displayed teacher or teacher selector, trace the value to its source.
Helper-only/test-only evidence is insufficient; verify real component wiring.

E. SEMANTIC MATRIX
Return:

| Semantic | Existing substrate | Runtime source | Writer | Reader | Historical behavior | Confidence | Gap |
|---|---|---|---|---|---|---|---|
| Teacher identity | | | | | | | |
| School access | | | | | | | |
| Class assignment | | | | | | | |
| Session assignment | | | | | | | |
| Substitute override | | | | | | | |
| Assignment audit | | | | | | | |
| Parent display teacher | | | | | | | |

F. ANSWER THESE QUESTIONS
1. Does a true class-level teacher assignment already exist?
2. Does a true session-level teacher assignment already exist?
3. If both exist, what is inheritance/override behavior?
4. Can multiple teachers be attached to one class?
5. Can a scheduled session use a different teacher?
6. Which teacher does Teacher Portal currently use to decide what a teacher sees?
7. Which teacher, if any, is stored on a submitted/completed session/report?
8. What happens to past sessions if class assignment changes?
9. Who can currently create/change/remove assignment?
10. Is assignment change audited?
11. What teacher is shown to parents today and from which exact source?
12. Is the current model semantically safe enough for P5 presentation only, or does it need new substrate?

G. GAP CLASSIFICATION
Choose primary gap:
- Gap Type 0 — already semantically correct;
- Gap Type 1 — presentation gap;
- Gap Type 2 — writer/projection gap;
- Gap Type 3 — semantic substrate gap;
- Gap Type 4 — authority/audit gap.
Multiple secondary gaps may be named.

H. RECOMMEND NEXT PHASE
Only after evidence:
- If no build needed: propose P5 closeout.
- If presentation-only: propose bounded P5.2 presentation build.
- If writer required on valid substrate: propose bounded writer build.
- If substrate/authority is unsafe: stop at design/decision gate before migration.

STOP CONDITIONS:
- Do not edit code.
- Do not create migrations.
- Do not change RLS/RPC.
- Do not deploy.
- Do not “fix” ambiguous semantics during audit.
- Stop and report any HEAD drift, package/tool pin drift, conflicting semantic fields, or authority ambiguity.

REQUIRED FINAL REPORT:
1. Authoritative HEAD + drift status.
2. Files/migrations/RPCs inspected.
3. Runtime-truth findings.
4. Semantic matrix.
5. Authority matrix.
6. Historical/reassignment behavior.
7. Parent-facing projection source.
8. Primary/secondary gap classification.
9. Exact next recommended phase.
10. Explicit statement: code changes = 0, migrations = 0, deployment = 0.
