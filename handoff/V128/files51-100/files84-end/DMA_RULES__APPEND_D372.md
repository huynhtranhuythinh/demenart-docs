<!-- ============================================================= -->
<!-- APPEND-ONLY BLOCK — paste VERBATIM at the END of DMA_RULES.md -->
<!-- Anchor: immediately AFTER the D371 delta line                 -->
<!--   ("… Khối D370/v1.58 … = HISTORICAL SNAPSHOT (BẤT BIẾN).")    -->
<!-- Byte-prefix through D371 is IMMUTABLE. Do not rewrite history. -->
<!-- ============================================================= -->

## 🗂️ D372 — V128-B11.2-A-AMENDMENT · EXECUTOR RESUME-PATH `class.edit` SUPPORT (Gate α — additive executor amendment; INERT: `class.edit` LOW, lifecycle 0/0, one-way door NOT opened) — IMPLEMENTATION CLOSEOUT — CANONICALIZED (2026-08-19)

> Implementation of the CTO-approved `V128-B11.2-A-AMENDMENT` design freeze (**Option 1 — scoped additive executor fix**). Closes the **G1 blocker** found at B11.2-A implementation-D1: the executor's **resume-approved** adapter dispatch handled only `class.assign.v1`; a `class.edit.v1` decision could `open → approve` but on **resume** fell through to `else → raise adapter_unresolved` → `MC_ACTION_EXECUTION_FAILED` (0 mutation), so no first committed proof was possible. **Gate α amends the executor ONLY and is INERT** — `class.edit` remains `LOW`, so the new resume arm is **unreachable** until Gate β elevates it; behaviour observable today = 0 change. Two-gate sequencing frozen: **Gate α** (this — executor fix) then **Gate β** (activation + first committed proof, deferred, separate re-gate). Applied via 1 migration; verified by JWT-impersonated **rolled-back** rehearsals (zero production residue). Supersedes D371 as current endpoint; D371/v1.59 (B11.1 inbox authority reconciliation) = HISTORICAL SNAPSHOT (BẤT BIẾN).

- **D372.1 — ⭐ Root cause = executor path asymmetry (B9.4 extended fresh only; resume is B7-era).** Fresh path (`v_inserted=true`) built `v_result` **per-adapter** and its `case v_adapter_key` handled both `class.assign.v1` **and** `class.edit.v1` (added B9.4/D368). The **resume-approved** path (`v_inserted=false`, decision `approved`, C-2 actor guard passed) still carried the **B7 shape**: `case` handled only `class.assign.v1`, built `v_result` **after** the `case` with a hard-coded `class.assign` payload, and its `when others` error map lacked the four `class.edit` domain codes. Dormant lifecycle (0/0) meant the resume-approved-HIGH branch had **never executed since B7**, hiding the gap — exactly the Risk-2 flagged at the B11.2-A freeze.

- **D372.2 — ⭐ Fix = additive, executor-body only (mirror the fresh path).** Single affected object `public.execute_mission_control_action`. In the **resume-approved** block: (a) **relocate** `v_result` construction **into each `case` arm** (the `class.assign.v1` arm now builds the same `{class_distribution_id}` + `CLASS_ASSIGNMENT_CREATED` payload it did before — byte-equivalent, replayed=true); (b) **add** `when 'class.edit.v1' then v_edit_result := public.class_edit_v1(p_object_id, p_input); v_result := {…, result:v_edit_result, audit:CLASS_UPDATED, replayed:true}`; (c) **add** four resume error mappings `no_editable_fields | name_invalid | age_group_invalid | level_invalid → MC_ACTION_INPUT_INVALID` (mirroring the fresh outer handler). The `else → raise 'adapter_unresolved'` fallthrough and all decision-lifecycle / begin/commit / fingerprint / C-2 logic are unchanged. `v_edit_result` was already declared (B9.4). No new variable, no signature change, no schema/registry/data change.

- **D372.3 — ⭐ Boundaries preserved (orchestration vs mutation; consume-only).** Executor stays **orchestration-only** — it *calls* the adapter, never `UPDATE`s `classes`. Mutation ownership stays with adapter `public.class_edit_v1` (DEFINER; domain-safety + whitelist `UPDATE classes` + `write_audit_log('CLASS_UPDATED')`), md5 **`63f3ab5a56f787f229a607ca30c48c99` UNCHANGED**. Resolver/decision-lifecycle/adapter/ledger invariants intact: **Resolver = WHO · Decision Lifecycle = WHETHER · Adapter = HOW · Ledger = execution truth · Memory = projection.**

- **D372.4 — ⭐ INERT property (the safety of Gate α).** `class.edit` risk stays **LOW** and `class.assign` stays **MEDIUM** → the new resume-approved `class.edit.v1` arm is **not reachable** in production (LOW never enters the HIGH/CRITICAL decision branch; MEDIUM auto-executes fresh). The amendment therefore produces **0 observable behaviour change** — it only makes the resume path *ready*. Governed decision lifecycle stays **DORMANT** (decisions/transitions **0/0**). **The one-way door (first immutable committed transition) is NOT opened by this block** — that is Gate β.

- **D372.5 — ⭐ Executor md5 changed; all frozen anchors UNCHANGED.** `execute_mission_control_action` md5 **`da733e9a636e9efd4457123e923057dd` → `2228611a65c3678d092b1c15ecc19c68`** (INVOKER preserved, `prosecdef=false`; ACL `{authenticated=EXECUTE, postgres=EXECUTE}` exact-restored — D15/D231 REVOKE ALL FROM PUBLIC/anon/authenticated/service_role → GRANT authenticated, VERIFY guard confirmed 0 leak). Regression-proven UNCHANGED: resolver `56b5e3f5a14360990b51c080ff1da1cb` · transition `fe0eea599db711fb8472b37a72fa25e4` · open_decision `b376edd70cc6fe4e83bf000dfa456954` · authority_gate `bc5ea1a2b1c090f6e4ec62da0a6dee0e` · class_edit_v1 `63f3ab5a56f787f229a607ca30c48c99` · resolve `bb79a5217c3a739e08986e2ec7d62bc0` · cancel `924c3f9ac9b80cfab6a65c00c927db0b` · inbox `af21682de9aac3415d51231d83dc14e7` · expire `c57c40b88b77e838da137dd3e9cf59e7`. **New baseline anchor recorded:** `public.assign_class_distribution` md5 **`03a1510bd827c03a650a3a88312fbe3a`** (not in DDL scope → definitionally unchanged; anchored now for future regression).

- **D372.6 — evidence (A/B/C/D/E; all rehearsals JWT-impersonated, sentinel-RAISE rolled back, zero residue).** Fixture (rolled-back): requester = platform `super_admin` (`profiles.id e86e45d1…`, user `446de75d…`, `platform_role`) · approver = demo-school master (`profiles.id 2fee5a07…`, user `5396961a…`, school `b6a4ac35…`, `organization_role`) — requester≠approver, enumeration `id<>requester` satisfied. **(A) class.assign regression PASS:** A1 fresh assign of an existing pair → `MC_ACTION_CONFLICT` (dispatch→`assign_class_distribution`→`distribution_exists`; NOT `adapter_unresolved`/`EXECUTION_FAILED`); A2 fresh assign of a free entitled pair (INSERT-only) → `ok=true` · `audit=CLASS_ASSIGNMENT_CREATED` · `result={class_distribution_id}`. **(B) class.edit@HIGH resume proof PASS:** open→`MC_ACTION_DECISION_REQUIRED` (pending, name unchanged) → approve (demo master, `approved`) → **resume `ok=true · replayed=true · audit=CLASS_UPDATED · result={changed_fields:[name]}` · class name changed**; decisions=1, transitions=2 `[NULL→pending, pending→approved]`. **(C) replay PASS:** 3rd call same request_id → `ok=false` + name stable → **0 duplicate decision / 0 duplicate transition / 0 net duplicate mutation** (consistent with class.assign replay semantics). **(D) adapter failure PASS:** resume with empty `{}` → `class_edit_v1` raises `no_editable_fields` → resume error map → `MC_ACTION_INPUT_INVALID` · name unchanged (subtxn rollback) · decision still `approved` (retryable) · transitions=2 (no dup) · ledger `processing` (retryable intermediate, not corrupt). **(E) hash + inert PASS** (D372.5 + registry LOW + 0/0). Post-run zero-residue confirmed live: decisions/transitions **0/0**, `class.edit` **LOW**, QA class name original, `mission_control_action_requests` **12** unchanged.

- **D372.7 — honest boundary (NO committed-decision claim; INERT).** Verified **through rolled-back JWT-impersonated rehearsal + runtime hash/ACL verification only**. The governed decision lifecycle remains **DORMANT** (0/0); **no committed production decision exists**; **no HIGH/CRITICAL action is activated**. No claim of "proven on committed data" — same epistemic discipline as B7/B8/B9.4/B10.1/B11.1. The resume `class.edit.v1` arm is proven **mechanically** (rehearsal) but is **runtime-unreachable** until Gate β.

- **D372.8 — ⭐ Gate β flags CARRIED (pre-existing B7 properties surfaced by this proof — NOT regressions; MUST be dispositioned before/at first committed decision).**
  1. **Approved-resume idempotency review.** The executor's approved-resume path **re-dispatches the adapter** on every replay (it does not short-circuit to the stored `result_payload` the way the non-HIGH replay branch does). `class.assign` is protected by `assign_class_distribution`'s uniqueness (`distribution_exists`/`unique_violation`→`MC_ACTION_CONFLICT`); **`class_edit_v1` has no uniqueness guard**, so a same-request_id replay re-invokes the edit with the **live** `p_input`. In rehearsal (identical input) the second `_mc_commit_action` failed on the already-terminal ledger row and the subtransaction rolled the re-edit back → 0 net effect. **Risk:** a replay carrying *different* field values would re-run a real edit before the commit-guard trips. **Candidate hardening (Gate β):** approved-HIGH replay should return the stored payload instead of re-dispatching, OR `class_edit_v1` gains an idempotency/version guard.
  2. **`intent_fingerprint` payload-binding review.** `intent_fingerprint` is forged from `(action_key, object, school[, program, lead])` and **excludes edited field values** (for `class.edit`, program/lead are null). An approved `class.edit` decision therefore authorizes the **intent to edit class X**, not a specific value set; resume re-reads live `p_input`, so the executed values need not equal those "intended" at open time. **Candidate hardening (Gate β):** bind field values into the fingerprint, or snapshot `p_input` at decision-open and execute the snapshot on resume (approver approves *what will run*).

- **D372.9 — rollback (fully reversible, function-body-only; NOT performed).** `CREATE OR REPLACE` `execute_mission_control_action` back to md5 `da733e9a636e9efd4457123e923057dd` + re-assert ACL (REVOKE ALL FROM PUBLIC/anon/authenticated/service_role → GRANT EXECUTE authenticated; owner postgres) + `NOTIFY pgrst`. **0 data-repair** (lifecycle 0/0; no schema/registry/data change). Reverting re-introduces the G1 resume-path gap — **acceptable while `class.edit` stays LOW** (resume branch unreachable until Gate β).

- **D372.10 — Deferred (NOT authorized by Gate α — Gate β + beyond, each needs explicit CTO authorization).** **Gate β** = registry `class.edit` risk **LOW→HIGH** activation · seed dedicated synthetic fixture (G4; pilots `d1…001`/`d2…001` off-limits) · **first COMMITTED decision** (opens the permanent one-way door 0/0 → 1/2, immutable by design) · disposition of the two D372.8 flags · data rollback + disarm + Gate β canonical closeout (next D-rule / SYSTEM_MAP bump). **Beyond:** `child.transfer` / object-scope expansion · Strangler Phase-3 (`assign_class_distribution` / class.assign executor embedded authority) · multi-approver/quorum · enum/taxonomy mint · `UNIQUE(user_id)` · delegation · notification · inbox UI · FE wiring for `class.edit` · Phase-4 (legacy admin RPCs).

**Delta:** **1 migration** (`20260819182957` `v128_b112a_amend_executor_resume_class_edit`, D92 3-block atomic: DDL body-replace → REVOKE/GRANT (D15/D231 ACL restore) → VERIFY fail-closed guard [INVOKER assert · 0 anon/service_role/PUBLIC leak · authenticated EXECUTE present · resume `class.edit.v1` arm present ≥2 occurrences] + `NOTIFY pgrst` D289). **Changed:** `execute_mission_control_action` md5 `da733e9a…`→`2228611a65c3678d092b1c15ecc19c68` (INVOKER preserved, ACL `{authenticated,postgres}` exact-restored). **Frozen (regression-proven UNCHANGED):** resolver `56b5e3f5…` · transition `fe0eea59…` · open_decision `b376edd7…` · authority_gate `bc5ea1a2…` · class_edit_v1 `63f3ab5a…` · resolve `bb79a521…` · cancel `924c3f9a…` · inbox `af21682d…` · expire `c57c40b8…` · assign_class_distribution `03a1510b…` (new anchor). **Inventory (NO schema change from D371/v1.59; function-body-only):** public tables **94** · fns **253** · secdef **241** · policies **169** · triggers **34** · cron **1**; `mc_internal` {**9** fn / **8** secdef} + **1** table. `mission_control_decisions` **0** · `mission_control_decision_transitions` **0** (DORMANT — one-way door NOT opened). Registry unchanged: **class.edit LOW active GATED** · class.assign MEDIUM active ungated · authority.probe LOW active ungated. Migration tail `20260819092150` → **`20260819182957`**. FE main pin `2.8.5`, 0 change. Endpoint: RULES **D372** · SYSTEM_MAP **v1.60** · HANDOFF **V128-B11.2-A-AMENDMENT-CLOSEOUT** · backend tail `20260819182957` · FE main pin `2.8.5`. Khối D371/v1.59 (B11.1 Decision Inbox Authority Reconciliation) = HISTORICAL SNAPSHOT (BẤT BIẾN).
