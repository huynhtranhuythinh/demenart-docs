---

## 🧩 D367 — V128-B9.3 · AUTHORITY RESOLVER SKELETON RECONCILIATION — CANONICALIZED (2026-08-16)

> **Endpoint mới:** RULES **D367** · SYSTEM_MAP **v1.55** · HANDOFF **V128-B9.3-SKELETON-RECONCILE** · backend tail **`20260815201925`** (`v128_b93_authority_resolver_skeleton`). Khối D366 phía trên = **HISTORICAL SNAPSHOT (BẤT BIẾN)**.
> **Mode:** DOCUMENTATION-ONLY reconciliation của một live implementation **đã applied**. **0 migration · 0 SQL · 0 DB mutation · 0 FE** trong khối này. Append-only.

- **D367.1 — Objective.** Reconcile live migration `20260815201925` (`v128_b93_authority_resolver_skeleton`) — Phase-1 Authority Resolver **skeleton** — vào canonical governance. Skeleton = first implementation của contract đóng băng ở D366/B9.1: resolver authorize **một** governed action **mới** end-to-end (`authority.probe`), zero legacy touch. Khối này **không** mở B9.4, **không** implement `child.transfer`, **không** migrate legacy executor, **không** mint enum/taxonomy.

- **D367.2 — Why reconciliation was required.** Trước phiên này canonical đứng ở RULES D366 / SYSTEM_MAP v1.54 / HANDOFF V128-B9.2 / backend tail `20260815182235`; D366.8 ghi B9.3 = **deferred, await CTO authorization**. Live DB audit (READ-ONLY) phát hiện backend tail đã tiến `20260815182235 → 20260815201925` với B9.3 objects hiện diện. Live state **ahead of canonical documentation** → cần reconcile để tài liệu khớp thực trạng. Audit read-only đã xác nhận: mọi object delta giải thích trọn vẹn, không residue, không rogue mutation.

- **D367.3 — Live implementation evidence (verified, not from memory).** Backend tail `20260815201925`, đúng **một** migration ahead of D366 baseline, không migration xen giữa. Migration structure = **D92 three-block** (DDL → ACL D15/D231 → VERIFY fail-closed `RAISE`) + `NOTIFY pgrst`. Inventory live (all re-pinned): tables(public) **94** (log table nằm ở `mc_internal`, không đụng public) · fns(public) **252** (+1) · secdef(public) **240** (+1) · policies **169** (+0) · cron **1** (+0) · `mc_internal` **{8 fn / 7 secdef}** (+1/+1). Object md5 verified: resolver `56b5e3f5a14360990b51c080ff1da1cb` · probe `59e7c5d4a8d1dda25d9466500da82d13`.

- **D367.4 — Resolver contract conformance (D366.1/.4) — PASS.** `mc_internal._resolve_authority(uuid,text,jsonb,jsonb)` = **STABLE · SECURITY DEFINER · `search_path=''` · ACL postgres-EXECUTE only** (KHÔNG leak anon/authenticated). Return shape `{eligible, authority_source, reason_codes}`. **Resolver OWNS:** authority policy verdict · eligibility · authority_source · reason_codes. **Resolver KHÔNG own:** domain mutation (STABLE — chỉ SELECT `profiles`, zero write) · RLS enforcement · adapter effects · lifecycle mutation · executor replacement. **Observation (non-blocking):** skeleton chưa branch theo `p_action_key` (policy hiện quyết bằng role + `object_context.school_id`); đúng phạm vi skeleton một-action, ghi nhận là open item — KHÔNG phải contract violation. Không có D-rule nào trong khối này mint enum/taxonomy.

- **D367.5 — Probe adapter boundary.** `public.execute_authority_probe(uuid,uuid)` = **SECURITY DEFINER · VOLATILE** (đúng — có INSERT audit) **· `search_path=''` · ACL authenticated + postgres** (client-callable RPC). Probe = **sole consumer** của resolver (caller graph verified: `_resolve_authority` chỉ được `execute_authority_probe` gọi). Log sink `mc_internal.authority_probe_log`: **RLS ON · 0 policy · grants revoked khỏi PUBLIC/anon/authenticated/service_role** → fail-closed, mọi write đi qua definer probe (definer-mediated). Registry: `object_type='authority'` · `action_key='authority.probe'` · `capability='authority.probe'` (= primary anchor D366.3) · `adapter_key='authority.probe.v1'` · `status='active'` · `risk_level='LOW'` · `execution_mode='single_domain_rpc'` · `audit_event='authority.probe.executed'` · `required_context={school_id:uuid, exclusive}`. `authority.probe` = **governed action MỚI** (Strangler Phase-1, D366.7), KHÔNG phải legacy site được migrate.

- **D367.6 — Legacy isolation proof — PASS.** `public.execute_mission_control_action` (md5 `09ef5f48f3318bfb53e126f3bc81d40a`) và `mc_internal._mc_transition_decision` (md5 `fe0eea599db711fb8472b37a72fa25e4`) **UNCHANGED**; cả hai **không** reference `_resolve_authority` / `execute_authority_probe` / `authority_probe_log` (source scan: false/false/false). Resolver **không thay** legacy executor; B8 lifecycle mutation boundary (`_mc_transition_decision`) nguyên vẹn. Governed legacy path vẫn dormant như D366.

- **D367.7 — Trigger inventory clarification (resolves audit anomaly).** Discovery ghi triggers 34→40 (+6). **Resolved = counting-scope mismatch (KHÔNG drift, KHÔNG rogue).** Canonical inventory line "triggers 34" đo **public-schema application triggers**; live audit đo all-schema = 40. Phân rã: **public 34** (= baseline chính xác) · cron 1 · realtime 1 · storage 4. **+6 = 6 system trigger do Supabase quản lý** (`cron.job`, `realtime.subscription`, `storage.buckets`×2, `storage.objects`×2), có sẵn từ khi provision extension, **chưa từng nằm trong DMA inventory**. **B9.3 tạo 0 trigger**; `authority_probe_log` không có trigger. **Convention chốt:** DMA trigger inventory = **public-schema only**; system-schema trigger (cron/realtime/storage) không tính vào application inventory.

- **D367.8 — Vocabulary status (DRAFT, KHÔNG minted — D366.6 preserved).** Skeleton phát sinh các giá trị observed: `authority_source ∈ {platform_role, organization_role, none}`; `reason_codes ∈ {actor_unresolved, self_decision_forbidden, authority_granted, object_context_mismatch, organization_scope_mismatch, role_not_eligible}`. Đây là **candidate/DRAFT**, **NOT minted** thành enum/CHECK/taxonomy. Final `authority_source` enum + `reason_code` taxonomy mint = **deferred post-B9.3 evidence** (D366.6). B9.1 §8.7 (mint-at-B9.2) vẫn SUPERSEDED.

- **D367.9 — Authorization provenance.** Owner operational authorization sequence is recorded as execution provenance. Canonical reconciliation does not claim external signature or independent approval artifact. (Cụ thể: Owner ra quyết định A — Canonicalize B9.3 — sau read-only audit; khối này ghi nhận điều đó như execution provenance, KHÔNG claim chữ ký ngoài, KHÔNG claim approval artifact độc lập tồn tại.)

- **D367.10 — ⭐ Open decisions (deferred, KHÔNG authorized bởi khối này).** (a) `authority_source` enum + `reason_code` taxonomy mint — post-B9.3 evidence. (b) `UNIQUE(user_id)` enforcement — sau reconciliation pass (re-verify duplicates=0 + D93 invite/link non-breakage). (c) `p_action_key` policy-branching khi resolver phục vụ >1 action. (d) Strangler Phase 3–4 legacy authority-site migration (≈51 `is_admin` / ≈29 hardcoded `master/sub_admin` / ≈18 `same_school`). (e) HIGH/CRITICAL activation · delegation · object-scope expansion (`child.transfer`, session/school) · multi-approver/quorum · notification · inbox UI. **Await explicit CTO next-milestone (B9.4 / first production governed action) authorization.**

**Delta:** DB **0 migration / 0 SQL / 0 mutation** trong khối này (reconcile của B9.3 đã-applied: +2 fn [`_resolve_authority`, `execute_authority_probe`] +1 table [`authority_probe_log`] +1 registry row [`authority.probe`], **+0 trigger**) · FE 0 · Edge 0 · Bunny 0. Inventory (public application): **94 · 252 · 240 · 169 · 34 · 1**; `mc_internal` {8 fn / 7 secdef}. Governed legacy path dormant. FE main pin `2.8.5`, 0 change. **Rollback (nếu reverse B9.3 implementation, ngoài phạm vi khối doc này):** DROP `public.execute_authority_probe(uuid,uuid)` + DROP `mc_internal._resolve_authority(uuid,text,jsonb,jsonb)` + DROP TABLE `mc_internal.authority_probe_log` + DELETE registry row `authority/authority.probe` + `NOTIFY pgrst`. **Reverse governance decision này = append superseding D-rule.** Endpoint: RULES **D367** · SYSTEM_MAP **v1.55** · HANDOFF **V128-B9.3-SKELETON-RECONCILE** · backend tail **`20260815201925`** · FE main pin `2.8.5`. Khối D366/v1.54 (B9.2 Authority Contract Ratification) = HISTORICAL SNAPSHOT (BẤT BIẾN).
