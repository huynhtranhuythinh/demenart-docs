# 🛡️ DMA_RULES.md — V128-P7 FINAL — CANON DELTA (append block)

> **How to apply:** This is the **P7 FINAL append block** for the live cumulative `~/dev/demenart-docs/canon` `DMA_RULES.md`.
> Set the block anchor to **`D‹live max + 1›`** (continue the live D-series; mounted reference snapshot ended at D379 but live is ahead — the P7.0–P7.3 rules already exist above this block). Append only; do **not** rewrite unrelated phases. Prior P7.0–P7.3 D-blocks become HISTORICAL SNAPSHOT (BẤT BIẾN); this FINAL block is the new current endpoint.

## 🗂️ D‹NEXT› — V128-P7 · POST-SESSION CHILD FOLLOW-UP — FINAL CLOSEOUT (release rehearsal ACL correction + production release) — CANONICALIZED (2026-08-31)

> Closes capability **V128-P7**. Backend lineage M1→M1.1→M2→**M3(P7.3.3)**; accepted FE `cfac171`. Release-rehearsal discovered an unintended `anon` EXECUTE surface on three P7 functions → **ACL-only** correction (migration 189). Production released to `demenart.com`; Owner-attested UI PASS + independent backend evidence. **Docs-only closeout — 0 product/DB mutation.**

- **D‹NEXT›.1 — ⭐ Final runtime pin = 189 / M3.** Backend tail is `20260831055744 v128_p7_3_3_follow_up_rpc_anon_revoke`; immediate prev `20260831023113 v128_p7_3_2_teacher_todo_follow_up_count`. **188/M2 is NOT the final P7 runtime tail** — M2 is documented as the TODO-count integration migration only. No migration 190+.
- **D‹NEXT›.2 — ⭐ Observation Truth ≠ Action State.** Historical `child_observations` stay pedagogical records; resolving a follow-up action never rewrites observation truth or action provenance.
- **D‹NEXT›.3 — ⭐ P7 action creation predicate is authoritative-closeout-signal-driven.** `attendance IN ('present','late')` AND (`needs_support` OR `follow_up_needed`). **Absent child creates NO P7 action.** `source_signal ∈ {needs_support, follow_up_needed, both}`.
- **D‹NEXT›.4 — ⭐ Lifecycle is narrow: open → resolved only.** No reopen / escalation / comments / assignment-queue / case states in P7.
- **D‹NEXT›.5 — ⭐ READ authority = source observer OR current distribution authority; RESOLVE authority = current distribution authority ONLY.** A historical source teacher does not retain resolve merely for having created the observation.
- **D‹NEXT›.6 — ⭐ Distribution authority = lead teacher OR temporal responsible-teacher precedence** (in_progress → nearest upcoming → most-recent past/overdue; excluding cancelled/rescheduled). Extends D324 (session responsibility is the authority source; no fallback to planned/taught_by/admin).
- **D‹NEXT›.7 — ⭐ Authority is fail-closed twice.** Helper `COALESCE(…, false)`; resolve boundary `authority IS NOT TRUE ⇒ forbidden`. Established by M1.1 after a SQL three-valued-logic bypass on NULL `lead_teacher_id` was found in rehearsal. **Lesson: SECURITY DEFINER authority predicates must COALESCE NULL→false and gate on IS NOT TRUE, never `= false` / `NOT`.**
- **D‹NEXT›.8 — ⭐ TODO count = OPEN actions caller can RESOLVE; never derived from readable-item count.** Source-observer read-only items must not inflate the actionable badge (`counts.open_follow_up_count` from RESOLVE authority, M2).
- **D‹NEXT›.9 — ⭐ P7.3.3 ACL least-privilege correction (release-rehearsal catch).** Three functions (`follow_up_distribution_authority(uuid)`, `resolve_child_follow_up(uuid)`, `get_teacher_open_follow_ups()`) carried unintended `anon`/`PUBLIC` EXECUTE. Corrected ACL-only (REVOKE PUBLIC+anon → GRANT authenticated/service_role/postgres). Final least-privilege state proven; **function bodies md5-unchanged** (`b96ba874…` / `50a202a2…` / `abf65603…`), signatures unchanged, RLS unchanged, trigger unchanged (exactly one `create_follow_up_actions`), **0 data mutation**, **0 frontend change** (accepted FE `cfac171` stays valid). Reinforces D15 (CREATE OR REPLACE / defaults can leave anon/PUBLIC — always REVOKE then GRANT in a dedicated block, verify with `aclexplode`) and D92 (3-block DDL→REVOKE/GRANT→VERIFY rollback guard) and D289 (`NOTIFY pgrst` after function/policy touch).
- **D‹NEXT›.10 — ⭐ Fail-closed on the outer layer too.** Even with fail-closed bodies, anon/PUBLIC EXECUTE on app-facing SECDEF RPCs is a least-privilege defect to REVOKE. App-facing RPC ACL = `authenticated + service_role + postgres` (no anon, no PUBLIC).
- **D‹NEXT›.11 — Parent has no P7 visibility; school/admin projection deferred; no notification in P7 core.**
- **D‹NEXT›.12 — ⚠️ DEFERRED HARDENING DEBT (non-blocking).** `get_teacher_todo_counts()` is SECURITY DEFINER with `SET search_path TO 'public'` (vs `''` convention). Deliberately preserved in P7; no exploit/semantic contradiction proven; references qualified. **Do NOT create migration 190 to change search_path in the P7 closeout.** Track as hardening debt.
- **D‹NEXT›.13 — ⚠️ RELEASE-EVIDENCE PROVENANCE (honest record).** Backend lineage/ACL/hash/trigger/RLS/CFA = **independently verified** on live production Supabase. Production deploy + UI screenshots = **Owner-attested**. Production behavioral cases needing an OPEN fixture (open=0) + destructive resolve smoke = **NOT EXERCISABLE — no fabricated PASS**. Independent production asset-SHA fingerprint = **not captured** (deploy identity via Owner attestation + `main`→Cloudflare auto-deploy, D105/D309.1).

**Delta:** **1 migration** (`v128_p7_3_3_follow_up_rpc_anon_revoke`, D92 3-block, ACL-only). **Changed:** ACL of 3 follow-up functions → least-privilege. **Frozen (VERIFY-asserted UNCHANGED):** bodies `b96ba874…` · `50a202a2…` · `abf65603…`; guard `get_teacher_todo_counts` `015279e2…`; `session_reports` trigger (1× `create_follow_up_actions`); RLS on `child_follow_up_actions` + `session_reports`. **Data:** CFA total=2 / open=0 / resolved=2 (0 mutation). **FE:** 0 change; accepted SHA `cfac171cdd1023bbf780688cab50ab3544f0fcce`; main pin `2.8.5`. Backend tail `20260831023113` → **`20260831055744`** (count **189**). Endpoint: RULES **D‹NEXT›** · SYSTEM_MAP **v‹live +1›** · HANDOFF **V128-P7** · backend tail `20260831055744` · FE SHA `cfac171` · FE pin `2.8.5`. Prior P7.0–P7.3 blocks = HISTORICAL SNAPSHOT (BẤT BIẾN).
